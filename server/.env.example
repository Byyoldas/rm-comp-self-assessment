# Optional. If unset, the app runs fully offline using the deterministic
# heuristic evidence evaluator instead of calling the Anthropic API.
ANTHROPIC_API_KEY=
RM_COMP_LLM_MODEL=claude-sonnet-5
PORT=4000
